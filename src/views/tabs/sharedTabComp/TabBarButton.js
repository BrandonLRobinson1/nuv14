import React from 'react';
import { TouchableOpacity, View, Text } from 'react-native';

// going to try to finess tab bar at some point
const TabBarButton = (props) => <View><Text>button</Text></View>

export default TabBarButton;
